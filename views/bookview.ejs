<!DOCTYPE html>
<html>
<head>
  <% include headerStuff %>
</head>
<body>
  <div class="container-fluid">
    <% include pageHeader %>

    <div class="row"><div class="col-xs-12">
      <p><span class="viewSpan">ISBN:</span> <%= book ? book.isbn : '' %></p>
      <p><span class="viewSpan">Title:</span> <%= book ? book.title : '' %></p>
      <p><span class="viewSpan">SubTitle:</span> <%= book ? book.subtitle : '' %></p>
      <p><span class="viewSpan">Author:</span> <%= book ? book.author : '' %></p>
      <p><span class="viewSpan">Genre:</span> <%= book ? book.genre : '' %></p>
      <p><span class="viewSpan">Been Read?:</span> <%= book ? book.read : '' %></p>
    </div></div>


      <% if (bookkey) { %>
        <div class="row"><div class="btn-group col-sm-12">
          <a class="btn btn-default" href="/books/destroy?key=<%= bookkey %>" role="button">Delete</a>
          <a class="btn btn-default" href="/books/edit?key=<%= bookkey %>" role="button">Edit</a>
        </div></div>
      <% } %>
    <% include footer %>
  </div>
</body>
</html>
