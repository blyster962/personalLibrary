CREATE TABLE IF NOT EXISTS books (
  bookkey   number,
  isbn      VARCHAR(25),
  title     VARCHAR(255),
  subtitle  VARCHAR(255),
  author    VARCHAR(255),
  genre     VARCHAR(50),
  read      VARCHAR(3)
);
